# Eval Evidence - PR #351 (Auto-Recap + Context Briefing)

Date: 2026-04-17 (backfilled 2026-04-18 per audit `tasks/codex-session2-audit-summary.md` §2.3)
PR: create-sentinelayer#351
Spec section: `docs/MULTI_AGENT_SESSION_SPEC.md` §PR 11
Scope trigger: LLM-driven session summarization running on timer.

## What changed
- `src/session/recap.js` — auto-recap emitter (LLM summarizes recent stream activity)
- `src/session/agent-registry.js` — context-briefing on agent join uses latest recap
- New `recap_emitted` event on session stream

## Eval impact assessment
- **Prompt changes:** YES — new recap summarization prompt template in `src/session/recap.js`
- **Model-route changes:** NO — inherits default provider + model from session config
- **Tool allowlist changes:** NO (LLM only produces text output)
- **Policy/routing changes:** NO

## Validation evidence
- `node --test tests/unit.session-recap.test.mjs` — 6 tests pass (initial emission, interval re-emission, dedup on no-activity, abort on budget cap, token accounting, recap visible to new joiners via context briefing)
- Omar Gate on merge: P0=0, P1=0, P2=6 (all CI/CD pattern)
- Manual smoke: 10-minute session with intermittent agent activity → 3 recap events emitted, each summarizing the prior ~3 min accurately.

## Risk summary
- **Primary risk:** recap prompt leaks raw secret content from stream events. Mitigated by payload redaction at stream sink (PR #357) — recap consumes redacted events only.
- **Secondary risk:** runaway cost on very long sessions. Mitigated by per-recap budget cap + exponential backoff on repeated no-op intervals.
- **Residual risk:** low.

## Follow-ups
- Add CLI-driven kill-switch test for recap emitter (shipped in PR #357).
- Add eval regression test for recap prompt stability (tracked in roadmap).
